import Image from "next/image";
import AppStore from "@/asset/app-svg/app-store.svg";

const IconAppStore = () => {
  return <Image src={AppStore} alt="icon-app-store" width={140} height={120} />;
};

export default IconAppStore;
